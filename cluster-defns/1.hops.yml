name: HopsClusterHA
baremetal:
    username: vagrant

cookbooks:       
  hops:                                                                     
    github: "smkniazi/hops-hadoop-chef"
    branch: "hopsfs-mount"  

attrs:
  install:
    dir: /srv/hops
  kagent:
    enabled: "false"
  hops:
    docker:
      enabled: "false"  
    yarn:
      aux_services: "mapreduce_shuffle"

groups: 
  master:
    size: 1 
    baremetal:
      ip: 10.0.2.15
    recipes: 
      - ndb::mgmd
      - ndb::mysqld
      - ndb::ndbd
      - hops::ndb
      - hops::nn
      - hops::dn
      - hops::rm
      - hops::nm
      - hops::fuse_mnt

